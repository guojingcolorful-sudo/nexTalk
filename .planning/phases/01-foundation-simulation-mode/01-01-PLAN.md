---
phase: 01-foundation-simulation-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - .gitignore
  - eslint.config.js
  - .prettierrc
  - playwright.config.ts
  - apps/desktop/package.json
  - apps/desktop/vite.config.ts
  - apps/desktop/tsconfig.json
  - apps/desktop/vitest.config.ts
  - apps/desktop/tailwind.config.js
  - apps/desktop/postcss.config.js
  - apps/teleprompter/package.json
  - apps/teleprompter/vite.config.ts
  - apps/teleprompter/tsconfig.json
  - apps/teleprompter/vitest.config.ts
  - apps/teleprompter/tailwind.config.js
  - apps/teleprompter/postcss.config.js
  - packages/design-tokens/package.json
  - packages/design-tokens/vitest.config.ts
  - packages/design-tokens/src/tokens.css
  - packages/design-tokens/src/tailwind-preset.js
  - packages/design-tokens/src/tokens.test.ts
  - packages/protocol/package.json
  - packages/protocol/vitest.config.ts
  - packages/protocol/src/index.ts
  - packages/protocol/src/index.test.ts
autonomous: false
requirements: [UI-01, UI-03, SYNC-01, SYNC-03]

must_haves:
  truths:
    - "A single `pnpm -r test` command runs all unit suites with green results"
    - "Both apps build to ES2022/Safari-15-compatible bundles (no Safari-16+ syntax)"
    - "Design token values match UI-SPEC exactly (hex values, shadows, type scale)"
    - "Protocol messages narrow correctly at runtime (invalid payloads rejected before render)"
  artifacts:
    - path: "packages/design-tokens/src/tokens.css"
      provides: "Design token custom properties (UI-01)"
      contains: "--color-darker-space"
    - path: "packages/protocol/src/index.ts"
      provides: "Shared WS message contract incl. `language` mode variant (SYNC-01, SYNC-03)"
      exports: ["ServerEvent", "ClientMessage", "LanguagePref", "isServerEvent"]
    - path: "apps/desktop/vite.config.ts"
      provides: "Safari 15.6 build target (UI-03)"
      contains: "safari15"
    - path: "apps/teleprompter/vite.config.ts"
      provides: "Safari 15.6 build target (UI-03)"
      contains: "safari15"
  key_links:
    - from: "apps/desktop/tailwind.config.js"
      to: "packages/design-tokens/src/tailwind-preset.js"
      via: "preset import"
      pattern: "design-tokens"
    - from: "packages/design-tokens/src/tokens.test.ts"
      to: "packages/design-tokens/src/tokens.css"
      via: "contract snapshot of token values"
      pattern: "portalGreen"
    - from: "packages/protocol/src/index.test.ts"
      to: "packages/protocol/src/index.ts"
      via: "narrowing assertions on ServerEvent/ClientMessage"
      pattern: "isServerEvent"
---

<objective>
**Phase Goal:** **As a** 求职者 preparing for an English interview, **I want to** run the complete NexTalk product — desktop app, LAN-synced phone teleprompter, and full simulated event flow — on mock audio, **so that** I can demo the entire experience end-to-end before any real audio pipeline exists.

Slice 01-01: the foundation contracts every later slice builds on — toolchain, pnpm workspace, Safari-15-safe build config, the design-token package, the shared WS protocol package, and the Wave 0 test harness mandated by VALIDATION.md.

Purpose: The dev machine has NO Rust toolchain and NO pnpm (research Pitfall 6). Nothing builds until those exist. Design tokens and protocol types are the contracts that the desktop UI (01-03), the H5 (01-04), and the Rust server (01-02) all consume — they must exist first and be tested first (interface-first ordering).
Output: Working monorepo with green test harness, exact-version lockfile, `packages/design-tokens` (tokens.css + Tailwind preset + contract test), `packages/protocol` (closed union types + narrowing + tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-simulation-mode/01-CONTEXT.md
@.planning/phases/01-foundation-simulation-mode/01-RESEARCH.md
@.planning/phases/01-foundation-simulation-mode/01-UI-SPEC.md
@.planning/phases/01-foundation-simulation-mode/01-VALIDATION.md
@.planning/phases/01-foundation-simulation-mode/SKELETON.md

This plan implements: locked decisions D-01 (React 19, not Vue), discretion items (monorepo shape, TS strict, Vitest, component naming source).
Environment blockers that MUST be handled here: Rust toolchain absent, pnpm absent, Playwright browsers absent (research "Environment Availability").
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve npm package legitimacy before any install</name>
  <what-built>Nothing installed yet. The research Package Legitimacy Audit (01-RESEARCH.md section "Package Legitimacy Audit") tags every npm package [ASSUMED] because slopcheck could not run. Per the security gate, a blocking human checkpoint precedes the first `pnpm install`.</what-built>
  <how-to-verify>
    1. Open https://npmjs.com/package/react, /vite, /tailwindcss, /@fortawesome/fontawesome-free, /@fontsource/space-grotesk, /@tauri-apps/api, /@tauri-apps/cli, /react-router-dom, /vitest, /@testing-library/react, /jsdom, /@playwright/test, /qrcode, /core-js, /ws, /typescript, /@vitejs/plugin-react, /concurrently, /postcss, /autoprefixer
    2. For each, confirm the source repository link matches the audit table (facebook/react, vitejs/vite, tailwindlabs/tailwindcss, FortAwesome/Font-Awesome, fontsource/font-files, tauri-apps/tauri, remix-run/react-router, vitest-dev/vitest, testing-library/react-testing-library, microsoft/playwright, soldair/node-qrcode, zloirock/core-js, websockets/ws, eslint, prettier, open-cli-tools/concurrently, postcss, browserlist/autoprefixer)
    3. Confirm weekly downloads are in the millions for the core set (matching the audit table magnitude) and no package shows a recent suspicious maintainer change or postinstall script beyond what the audit verified
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installs, or list any package you want excluded/re-verified</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install toolchain and scaffold the pnpm workspace with Safari-15-safe configs</name>
  <files>pnpm-workspace.yaml, package.json, .gitignore, eslint.config.js, .prettierrc, playwright.config.ts, apps/desktop/package.json, apps/desktop/vite.config.ts, apps/desktop/tsconfig.json, apps/desktop/vitest.config.ts, apps/desktop/tailwind.config.js, apps/desktop/postcss.config.js, apps/teleprompter/package.json, apps/teleprompter/vite.config.ts, apps/teleprompter/tsconfig.json, apps/teleprompter/vitest.config.ts, apps/teleprompter/tailwind.config.js, apps/teleprompter/postcss.config.js, packages/design-tokens/package.json, packages/design-tokens/vitest.config.ts, packages/protocol/package.json, packages/protocol/vitest.config.ts</files>
  <read_first>
    - 01-RESEARCH.md sections "Standard Stack", "Installation", "Environment Availability", "Common Pitfalls 1/6"
    - 01-UI-SPEC.md section "Design System" (styling stack lock: Tailwind v3.4, no v4 features)
  </read_first>
  <action>
    Install the Rust toolchain via the official rustup script and verify `rustc --version` and `cargo --version` work. If `cargo build` on a smoke crate fails against Xcode 14.2, pin a compatible toolchain with `rustup toolchain install 1.85 && rustup default 1.85` (research Open Question 2). Install pnpm via `corepack enable && corepack prepare pnpm@latest --activate`; if corepack is blocked, fall back to `npm install -g pnpm`.

    Create the pnpm workspace at repo root with `packages: ['apps/*', 'packages/*']`. Root package.json scripts: `dev` = `concurrently "pnpm --filter @nextalk/desktop dev" "pnpm --filter @nextalk/teleprompter build --watch"` (starts the two dev web servers — the desktop Vite dev server on 1420 and the teleprompter watch build; NEVER include `tauri dev` inside any dev script — Tauri's beforeDevCommand must not recurse into itself; 01-02 sets beforeDevCommand to this same pair), `build` = `pnpm --filter @nextalk/teleprompter build && pnpm --filter @nextalk/desktop build`, `test` = `pnpm -r test`, `test:e2e` = `pnpm exec playwright test`. Add `workspace:*` dependency links.

    Create the four package dirs with exact versions from the research Standard Stack (all confirmed on registry 2026-08-27): react/react-dom 19.2.8, typescript 5.9.3, vite 7.3.6, @vitejs/plugin-react 6.1.0, tailwindcss 3.4.19 (NOT 4.x — v4 CSS breaks Safari 15.6), postcss 8.5.26, autoprefixer 10.5.4, @fontsource/space-grotesk 5.3.0, @fortawesome/fontawesome-free 6.7.2 (PIN @6 — FA7 renames icons the UI-SPEC locks), @fortawesome/free-solid-svg-icons 6.7.2, @fortawesome/fontawesome-svg-core 6.7.2, @fortawesome/react-fontawesome 3.5.0, @tauri-apps/api 2.11.1, @tauri-apps/cli 2.11.4, qrcode 1.5.4, react-router-dom 7.18.2, vitest 4.1.11, @testing-library/react 16.3.2, jsdom 30.0.1, @playwright/test 1.62.1, core-js (latest). Add `ws` as a devDependency of apps/teleprompter (mock WS server for e2e — Node's native WebSocket is client-only). Add `concurrently` as a devDependency of BOTH root and apps/desktop (root: the `dev` convenience script; apps/desktop: Tauri's beforeDevCommand runs from the app dir and must resolve it there).

    Both apps' vite.config.ts MUST set `build: { target: ['safari15', 'es2022'] }` (Vite 7 default is Safari 16+ — research Pitfall 1; omitting this breaks the packaged WKWebView). Desktop dev server port 1420 strictPort. Teleprompter `base: './'` for LAN static serving. Both tsconfigs: strict true, moduleResolution bundler, target ES2022, jsx react-jsx.

    Tailwind: v3.4 pipeline (tailwind.config.js content globs + postcss.config.js with tailwindcss + autoprefixer) in both apps; the config `preset` wiring to @nextalk/design-tokens happens in Task 3 — leave configs ready to receive it.

    Test harness: `vitest.config.ts` per package/app (jsdom environment for the two apps, node for packages) — set `passWithNoTests: true` in EVERY vitest config: this task's verify runs `pnpm -r test` before any test files exist (Task 3 creates them), and vitest exits 1 on empty suites without the flag. Every package and app package.json gets `"test": "vitest run"` (so `pnpm -r test` and `pnpm --filter @nextalk/desktop test` work); apps/desktop additionally gets `"dev": "vite"` (the desktop dev server Tauri's devUrl loads — port 1420 strictPort per its vite.config.ts) and `"build": "vite build"`; apps/teleprompter gets `"build": "vite build"`. `playwright.config.ts` at root with projects desktop + teleprompter, baseURL per app, and `webServer` entries that run the built apps. Then `pnpm install` (single lockfile), then `pnpm exec playwright install chromium`.

    ESLint + Prettier: flat config (eslint.config.js) + .prettierrc at root, react-hooks and typescript plugins, no CDN anyplace. .gitignore: node_modules, dist, src-tauri/target, playwright-report, test-results.
  </action>
  <verify>
    <automated>pnpm -r test && cargo --version && rustc --version && pnpm exec playwright install chromium && node -e "const v=require('./package.json'); if(v.packageManager!=='pnpm') process.exit(1)"</automated>
  </verify>
  <done>
    - `rustc --version` and `cargo --version` print a working toolchain (pinned to 1.85 if the 2026 stable refused Xcode 14.2)
    - `pnpm -r test` exits 0 across all four packages with the configured harness (passWithNoTests swallows the pre-test-file window)
    - `apps/desktop/vite.config.ts` and `apps/teleprompter/vite.config.ts` both contain `safari15` in the build target array
    - `apps/desktop/package.json` has `"dev": "vite"` and `"test": "vitest run"` scripts; every package/app has a `test` script; concurrently installed at root AND apps/desktop
    - `@fortawesome/*` packages are pinned to `6.7.2` in package.json (verify: `pnpm list @fortawesome/fontawesome-free | grep 6.7.2`)
    - `tailwindcss` resolves to `3.4.19` (verify: `pnpm list tailwindcss`)
    - Playwright chromium browser downloaded
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the design-token and protocol contract packages with Wave 0 tests</name>
  <files>packages/design-tokens/src/tokens.css, packages/design-tokens/src/tailwind-preset.js, packages/design-tokens/src/tokens.test.ts, packages/protocol/src/index.ts, packages/protocol/src/index.test.ts, apps/desktop/tailwind.config.js, apps/teleprompter/tailwind.config.js, apps/desktop/package.json, apps/teleprompter/package.json</files>
  <read_first>
    - 01-UI-SPEC.md sections "Design System", "Spacing Scale", "Typography", "Color", "Layout & Container Contract" (authoritative token values — the checker approved these exact values on 2026-08-27)
    - 01-RESEARCH.md "Code Example 4" (protocol type design) and "Pattern 3" (script schema), "Validation Architecture" Wave 0 gaps
  </read_first>
  <behavior>
    - Test 1 (tokens contract): `tokens.css` declares every token below with the EXACT value from UI-SPEC; test reads the parsed custom properties and asserts equality (portalGreen #97ce4c, mortyYellow #fbf061, rickBlue #00b5cc, darkerSpace #151519, spaceDark #1E1E24, #1A1A22, slate-800 #1E293B, slate-700 #334155, red-500 #ef4444, black #000000, white #FFFFFF)
    - Test 2 (shadows): black 4px offset 4px, colored 6px offset per functional color, mobile 8px — assert via custom property values
    - Test 3 (narrowing valid): `isServerEvent({t:'subtitle', id:'s1', speaker:'interviewer', seq:1, en:'hello', final:true})` → true; `isServerEvent({t:'control', language:'bilingual'})` → false (client message, not server event)
    - Test 4 (narrowing invalid): missing `t` → false; unknown `t` value → false; `seq` not a number → false; speaker not in union → false; bullets not string[] → false
    - Test 5 (language variant): `isServerEvent({t:'language', language:'all-en'})` → true; `isServerEvent({t:'language'})` → false (missing language field); `{t:'language', language:'french'}` → false (not in LanguagePref union)
  </behavior>
  <action>
    packages/design-tokens (name @nextalk/design-tokens, main/src/index points at tokens.css + tailwind-preset.js):
    - tokens.css: declare under `:root` the full UI-SPEC token set with plain hex values (Safari 15.6-safe, NO oklch/color-mix): color tokens (darker-space #151519, space-dark #1E1E24, panel #1A1A22, slate-800 #1E293B, slate-700 #334155, portal-green #97ce4c, morty-yellow #fbf061, rick-blue #00b5cc, red #ef4444, ink #000000, paper #FFFFFF), shadow tokens (shadow-black-4 4px_4px_0_0_#000, shadow-black-8 8px_8px_0_0_#000, shadow-green-6 6px_6px_0_0_#97ce4c, shadow-yellow-6, shadow-blue-6), type scale (panel-title 18px/28px/700, dialogue 15px/600, translation 13px/700, label 12px/16px/700, mini 10px/14px/700), spacing scale (--space-xs 4px ... --space-3xl 64px), radii (--radius-card 12px, --radius-window 24px, --radius-mobile 40px, --radius-full 9999px), font stack (`Space Grotesk` first), dot-matrix background-image value (radial-gradient(#4a4a5c 1px, transparent 1px) with background-size 20px 20px), and the 6px hard-shadow family
    - tailwind-preset.js: export `theme.extend` mapping token names to Tailwind keys — colors { portalGreen, mortyYellow, rickBlue, darkerSpace, spaceDark }, boxShadow { cartoon-green/blue/yellow 6px offset, cartoon-black 4px, cartoon-black-lg 8px }, fontFamily sans Space Grotesk, backgroundImage dot-matrix — so both apps' tailwind.config.js can `preset: [require('@nextalk/design-tokens/src/tailwind-preset.js')]` and import tokens.css in their entry CSS
    - Wire the preset into BOTH apps IN THIS TASK (the wiring must land now — downstream plans 01-02/01-03/01-04 render token classes portalGreen/mortyYellow/rickBlue that only resolve via the preset): run `pnpm --filter @nextalk/desktop add @nextalk/design-tokens --workspace && pnpm --filter @nextalk/teleprompter add @nextalk/design-tokens --workspace` (adds workspace:* entries to both apps' package.json), then set `preset: [require('@nextalk/design-tokens/src/tailwind-preset.js')]` in BOTH apps/desktop/tailwind.config.js and apps/teleprompter/tailwind.config.js, and confirm each app's entry CSS imports tokens.css (add the import if the Task 2 scaffold left it out)
    - tokens.test.ts: contract snapshot asserting the exact hex values and shadow strings above (fail loudly on drift from UI-SPEC)

    packages/protocol (name @nextalk/protocol): src/index.ts implements the exact contract from research Code Example 4:
    - `export type LanguagePref = 'all-zh' | 'all-en' | 'bilingual'`
    - `export type Speaker = 'interviewer' | 'user'`
    - `ServerEvent` closed union: subtitle { t:'subtitle', id:string, speaker:Speaker, seq:number, zh?:string, en?:string, final:boolean }, strategy { t:'strategy', id:string, roundId:string, title:string, bullets:string[] }, status { t:'status', session:'idle'|'listening'|'generating'|'ended' }, language { t:'language', language:LanguagePref } — the applied language-mode observation event (SYNC-03 round-trip): the Rust server (01-05) emits it when it applies an inbound control message, so desktop + phone surfaces re-render in the new mode through the SAME ServerEvent channel (one event model, two transports), timeline { t:'timeline', events:ServerEvent[] }
    - `ClientMessage` closed union: control { t:'control', language:LanguagePref } (SYNC-03), resume { t:'resume', sinceSeq:number } — these EXACT field names are the cross-plan contract: the Rust server parses with deny_unknown_fields and drops connections on parse failure, so no plan may rename them (this is why the checker requires one scheme across 01-01/01-02/01-04/01-05)
    - `isServerEvent(x: unknown): x is ServerEvent` — discriminated narrowing that validates field types (seq is finite number, speaker ∈ union, bullets is string[], final is boolean, language ∈ LanguagePref) so invalid/malicious WS payloads never reach renderers (security V5)
    - index.test.ts covering the behavior cases above
  </action>
  <verify>
    <automated>pnpm --filter @nextalk/design-tokens test && pnpm --filter @nextalk/protocol test && grep -q 'design-tokens' apps/desktop/tailwind.config.js && grep -q 'design-tokens' apps/teleprompter/tailwind.config.js</automated>
  </verify>
  <done>
    - Both package test suites pass (incl. the `language` variant narrowing cases)
    - tokens.css contains the exact hex values from UI-SPEC (test-asserted, not eyeballed)
    - protocol exports the full ServerEvent/ClientMessage/LanguagePref/isServerEvent surface including the `language` variant; narrowing rejects malformed payloads per the behavior cases
    - Both apps' tailwind.config.js require the design-tokens preset (grep gate green); token classes resolve in downstream plans
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → workspace | Untrusted third-party code enters the build (package legitimacy) |
| WS wire → renderer (later plans) | Untrusted messages cross into React renderers — narrowing contract is defined HERE in packages/protocol |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (all packages [ASSUMED] — slopcheck unavailable) | mitigate | Task 1 blocking-human checkpoint verifying every audit-table package on npmjs.com before `pnpm install`; all Rust crates verified via crates.io API by research — no checkpoint needed for cargo |
| T-01-01 | Spoofing | WS pairing token (SYNC-01) | mitigate (later plan) | 128-bit OsRng token per session, defined in plan 01-02; protocol contract (this plan) fixes the message shapes the token gate protects |
| T-01-02 | Tampering | WS payloads → renderers | mitigate (this plan) | `isServerEvent` closed-union narrowing with per-field type validation in packages/protocol; every renderer MUST pass inbound messages through it (enforced in 01-02/01-04 tasks); Rust side mirrors with serde deny_unknown_fields |
| T-01-03 | Information disclosure | Subtitle content on LAN HTTP | accept | Locked LAN-demo tradeoff (research Security Domain); session-scoped token + no-store headers (01-02); no PII caches |
</threat_model>

<verification>
- Wave gate: `pnpm -r test` green across all four packages
- `pnpm list tailwindcss @fortawesome/fontawesome-free` shows 3.4.19 / 6.7.2
- `cargo --version` and `rustc --version` resolve (toolchain blocker cleared)
</verification>

<success_criteria>
- Toolchain blockers (rustup, pnpm) cleared on this machine; cargo smoke build passed
- pnpm workspace with 4 packages, single lockfile, all versions pinned per research Standard Stack
- `build.target: ['safari15','es2022']` present in BOTH vite configs (UI-03)
- design-tokens + protocol packages published as workspace packages with green contract tests (UI-01, SYNC-01/03 type surface)
- Playwright chromium downloaded; playwright.config.ts exists
</success_criteria>

<output>
Create `.planning/phases/01-foundation-simulation-mode/01-01-SUMMARY.md` when done
</output>
