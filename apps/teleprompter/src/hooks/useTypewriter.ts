import { useEffect, useState } from 'react';

// RED stub — deterministic cadence lands in the GREEN commit.
export function useTypewriter(text: string, _intervalMs = 40): string {
  void useEffect;
  void useState;
  return '';
}
